---
phase: 02-map-visualization
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/map/PortLayer.tsx
  - components/map/__tests__/PortLayer.test.tsx
  - app/page.tsx
autonomous: false
requirements: [VIZ-02]

must_haves:
  truths:
    - "All 86 ports render as small circular markers on the map"
    - "Hovering a port shows its name label"
    - "Clicking a port highlights it with a distinct color and persistent label"
    - "Selected ports are visually distinguishable from unselected ones"
    - "Map with ports is visible when visiting the app in a browser"
  artifacts:
    - path: "components/map/PortLayer.tsx"
      provides: "Port marker rendering with hover and selection state"
      exports: ["PortLayer"]
    - path: "components/map/__tests__/PortLayer.test.tsx"
      provides: "Tests for port rendering, selection, and hover behavior"
    - path: "app/page.tsx"
      provides: "App entry point wiring NauticalMap with port data"
  key_links:
    - from: "components/map/PortLayer.tsx"
      to: "@/lib/ports/types"
      via: "Port interface import"
      pattern: "import.*Port.*from.*ports/types"
    - from: "components/map/NauticalMap.tsx"
      to: "components/map/PortLayer.tsx"
      via: "PortLayer rendered inside SVG"
      pattern: "<PortLayer"
    - from: "app/page.tsx"
      to: "components/map/NauticalMap.tsx"
      via: "NauticalMap component usage"
      pattern: "<NauticalMap"
    - from: "app/page.tsx"
      to: "@/lib/ports/ports"
      via: "getAllPorts() call for port data"
      pattern: "getAllPorts"
---

<objective>
Add port markers to the nautical map with hover/selection interactivity, wire the map into the app page, and verify the full visual result.

Purpose: Completes VIZ-02 (port markers with labels and selection highlighting) and makes the map visible in the running application. This is the "hero moment" where users first see the beautiful nautical chart with all ports.

Output: PortLayer component, wired NauticalMap on app/page.tsx, visual checkpoint confirmation.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-map-visualization/02-RESEARCH.md
@.planning/phases/02-map-visualization/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From components/map/types.ts:
```typescript
export interface MapDimensions { width: number; height: number; }
export interface PortMarkerState { selectedIds: Set<string>; hoveredId: string | null; }
```

From components/map/constants.ts:
```typescript
export const NAUTICAL_COLORS = {
  portDefault: "#5c4a32", portSelected: "#c0392b",
  portHover: "#d4a017", portLabel: "#3d2b1f",
  labelSerif: "Georgia, 'Times New Roman', serif",
  // ... full palette
} as const;
```

From components/map/NauticalMap.tsx:
```typescript
// "use client" component
interface NauticalMapProps {
  width?: number; height?: number;
  selectedPortIds?: Set<string>;
  onPortSelect?: (portId: string) => void;
}
export function NauticalMap(props: NauticalMapProps): JSX.Element;
```

From lib/ports/types.ts:
```typescript
export interface Port {
  id: string; name: string; lat: number; lng: number;
  lake: GreatLakeName; type: "city" | "marina" | "island" | "landmark";
  state?: string; country: "US" | "CA";
}
```

From lib/ports/ports.ts:
```typescript
export function getAllPorts(): Port[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PortLayer with hover/selection and wire into NauticalMap and page</name>
  <files>
    components/map/PortLayer.tsx,
    components/map/__tests__/PortLayer.test.tsx,
    components/map/NauticalMap.tsx,
    app/page.tsx
  </files>
  <behavior>
    - Test: renders 86 SVG circle elements (one per port)
    - Test: default ports have small radius (r=3) and portDefault fill color
    - Test: selected port has larger radius (r=6), portSelected fill, white stroke
    - Test: hovering a port shows a text label with the port name
    - Test: clicking a port calls onSelect callback with port id
    - Test: unselecting by clicking a selected port again toggles it off
  </behavior>
  <action>
    **1. Create `components/map/__tests__/PortLayer.test.tsx` (RED first):**
    Write tests per the behavior block above. Use @testing-library/react. Create a mock projection function that returns [100, 100] for any input. Create a mock GeoPath. Test rendering, selection state, hover state, and click callbacks.

    **2. Create `components/map/PortLayer.tsx` (GREEN):**
    Props: `{ ports: Port[], projection: GeoProjection, selectedIds: Set<string>, hoveredId: string | null, onSelect: (id: string) => void, onHover: (id: string | null) => void }`.
    Implementation:
    - Render `<g className="port-layer">` wrapping all port markers
    - For each port: project [port.lng, port.lat] to SVG coordinates (CRITICAL: lng first, lat second per Pitfall 1)
    - Handle null projection result (skip port if projection returns null)
    - Default state: circle r=3, fill=portDefault, opacity=0.6, cursor=pointer
    - Selected state: circle r=6, fill=portSelected, stroke=white, strokeWidth=1.5, opacity=1, optional filter="url(#port-glow)"
    - Hovered (not selected): circle r=4, fill=portHover, opacity=0.8
    - Show text label (port.name) when hovered OR selected: positioned x+8, y+4 from center, serif font, fontSize=11, fill=portLabel
    - onClick calls onSelect(port.id), onMouseEnter calls onHover(port.id), onMouseLeave calls onHover(null)
    - Use NAUTICAL_COLORS from constants

    **3. Update `components/map/NauticalMap.tsx`:**
    - Add ports prop: `ports?: Port[]` (optional, for when called without ports)
    - Add internal state: `const [hoveredPortId, setHoveredPortId] = useState<string | null>(null);`
    - Accept selectedPortIds and onPortSelect from props (parent manages selection state)
    - Render `<PortLayer>` AFTER CoastlineLayer and BEFORE CompassRose in SVG layer order:
      coastlines -> ports -> compass rose (ports need to be above land, compass rose above everything)
    - Pass projection (not path) to PortLayer for coordinate projection

    **4. Update `app/page.tsx`:**
    - "use client" directive
    - Import NauticalMap from "@/components/map/NauticalMap"
    - Import getAllPorts from "@/lib/ports/ports"
    - Manage selectedPortIds state: `useState<Set<string>>(new Set())`
    - Toggle handler: if port already selected, remove from set; if not, add to set
    - Load ports via getAllPorts() in useMemo
    - Render full-width layout with NauticalMap as the main content
    - Use Tailwind for layout: flex column, min-h-screen, bg-neutral-100
    - Simple header with app title "Great Lakes Cruise Plotter" in sans-serif
    - Map container that fills available space

    **Coordinate order CRITICAL:** Always `projection([port.lng, port.lat])` -- d3-geo uses [longitude, latitude]. Swapping gives wrong positions.
  </action>
  <verify>
    <automated>cd /Users/luke/great-lakes-cruise-plotter && npx vitest run components/map/__tests__/ --reporter=verbose</automated>
  </verify>
  <done>
    - All 86 ports render as circle markers on the map
    - Selected ports show red highlight with label
    - Hovered ports show gold highlight with label
    - Clicking toggles selection state
    - app/page.tsx shows the full nautical map with ports
    - All component tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete nautical chart</name>
  <action>
    Human verifies the complete nautical chart SVG map renders correctly with all styling elements and port interactivity.
  </action>
  <what-built>
    Complete nautical chart SVG map of the Great Lakes with:
    - Parchment/cream land fills and blue water with depth gradient
    - All 5 Great Lakes coastlines rendered from TopoJSON data
    - Lat/lng graticule grid with degree labels
    - Compass rose in Lake Superior
    - 86 port markers as small dots
    - Hover shows port name, click highlights port in red
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/luke/great-lakes-cruise-plotter && npx next dev` to start dev server
    2. Open http://localhost:3000 in browser
    3. Verify nautical chart styling:
       - Background is blue (water) with subtle depth gradient
       - Land masses are parchment/cream colored
       - Coastlines have dark strokes
       - Dashed lat/lng grid lines visible on water areas
       - Compass rose visible in Lake Superior area
    4. Verify port markers:
       - Small dots visible across all 5 Great Lakes
       - Hover over a port -- name label appears
       - Click a port -- it turns red/highlighted with persistent label
       - Click again -- it deselects
    5. Verify all 5 lakes are present: Superior, Michigan, Huron, Erie, Ontario
    6. Verify map fills the main content area responsively
  </how-to-verify>
  <verify>User confirms visual quality and interactivity</verify>
  <done>User types "approved" confirming the nautical chart looks and works correctly</done>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass (including Phase 1 tests)
- `npx next build` -- clean build, no errors
- Visual inspection via dev server confirms nautical styling and port interactivity
</verification>

<success_criteria>
- VIZ-02 complete: all 86 ports displayed as markers, selected ports highlighted with labels
- Map is the main content of the app at localhost:3000
- Hover reveals port name, click toggles selection with visual highlight
- Full test suite green
- User approves visual quality at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-map-visualization/02-02-SUMMARY.md`
</output>

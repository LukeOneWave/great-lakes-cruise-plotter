---
phase: 04-route-planning-ui
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/route-planner/TripSummary.tsx
  - components/route-planner/SpeedControl.tsx
  - components/route-planner/RoutePlannerPanel.tsx
  - components/route-planner/__tests__/TripSummary.test.tsx
  - components/route-planner/__tests__/SpeedControl.test.tsx
  - app/page.tsx
autonomous: false
requirements: [ROUTE-03, ROUTE-04]

must_haves:
  truths:
    - "User sees total distance in nautical miles"
    - "User sees total estimated travel time"
    - "User sees per-leg breakdown with From, To, distance, and time"
    - "User can adjust cruise speed via slider (5-30 knots, default 10)"
    - "Travel time updates immediately when speed changes"
  artifacts:
    - path: "components/route-planner/TripSummary.tsx"
      provides: "Per-leg breakdown table and total trip summary"
      exports: ["TripSummary"]
    - path: "components/route-planner/SpeedControl.tsx"
      provides: "Speed slider/input in knots"
      exports: ["SpeedControl"]
  key_links:
    - from: "app/page.tsx"
      to: "components/route-planner/RoutePlannerPanel.tsx"
      via: "speedKnots state and routeLegs passed as props"
      pattern: "speedKnots.*setSpeedKnots"
    - from: "components/route-planner/TripSummary.tsx"
      to: "lib/pathfinding/distance.ts"
      via: "formatTime for per-leg and total time display"
      pattern: "formatTime"
---

<objective>
Trip summary display and cruise speed control: per-leg breakdown, total distance/time, and adjustable speed slider.

Purpose: Give users full trip details so they can plan realistic cruise itineraries with time estimates.
Output: Complete route planning panel with stop list, trip summary table, and speed control.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-route-planning-ui/04-CONTEXT.md
@.planning/phases/04-route-planning-ui/04-RESEARCH.md
@.planning/phases/04-route-planning-ui/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From lib/pathfinding/types.ts:
```typescript
export interface RouteLeg {
  from: Port;
  to: Port;
  path: PathResult | null;
  distanceNm: number;
}
```

From lib/pathfinding/distance.ts:
```typescript
export function computeDistanceNm(points: RoutePoint[]): number;
export function formatTime(distanceNm: number, speedKnots: number): string;
```

From components/route-planner/RoutePlannerPanel.tsx:
```typescript
interface RoutePlannerPanelProps {
  stops: Port[];
  routeLegs: RouteLeg[];
  speedKnots: number;
  onSpeedChange: (speed: number) => void;
  onReorder: (from: number, to: number) => void;
  onRemove: (index: number) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TripSummary and SpeedControl components</name>
  <files>components/route-planner/TripSummary.tsx, components/route-planner/SpeedControl.tsx, components/route-planner/__tests__/TripSummary.test.tsx, components/route-planner/__tests__/SpeedControl.test.tsx</files>
  <behavior>
    TripSummary:
    - Renders "No route yet" when routeLegs is empty
    - Shows total distance in nm (e.g., "245.3 nm") and total time (e.g., "24h 32m")
    - Shows per-leg table rows: from name, to name, distance nm, estimated time
    - Handles null path leg gracefully (shows "No route" for that leg)

    SpeedControl:
    - Renders slider with current value displayed
    - Range 5-30 knots, default value from props
    - Calls onSpeedChange when slider value changes
    - Shows "knots" label
  </behavior>
  <action>
1. Write tests FIRST in `components/route-planner/__tests__/TripSummary.test.tsx`:
- Test: renders empty state when no legs
- Test: renders total distance and time with mock legs
- Test: renders per-leg rows with from/to names
- Test: handles leg with null path (shows "No route")
Use @testing-library/react render + screen queries. Mock RouteLeg data with known distances.

2. Write tests FIRST in `components/route-planner/__tests__/SpeedControl.test.tsx`:
- Test: renders slider with current speed value
- Test: displays speed in knots
- Test: calls onSpeedChange when slider changes

3. Create `components/route-planner/TripSummary.tsx`:
- "use client" directive
- Props: `routeLegs: RouteLeg[]`, `speedKnots: number`
- Import `formatTime` from `@/lib/pathfinding/distance`
- Compute totals: `totalDistanceNm = routeLegs.reduce(sum of distanceNm)`, `totalTime = formatTime(totalDistanceNm, speedKnots)`
- Render: prominent total distance and time at top, then a table/list of per-leg breakdowns
- Per-leg row: leg number, from.name, to.name, distanceNm formatted to 1 decimal, formatTime for that leg
- If leg.path is null: show "No route found" in italics for that row instead of distance/time
- Nautical-themed styling with Tailwind: compact table, subtle borders, text-sm

4. Create `components/route-planner/SpeedControl.tsx`:
- "use client" directive
- Props: `speedKnots: number`, `onSpeedChange: (speed: number) => void`
- Render: label "Cruise Speed", range input (min=5, max=30, step=1), current value display "{N} knots"
- Styling: full-width slider, value displayed prominently
- onChange: calls `onSpeedChange(Number(e.target.value))`
  </action>
  <verify>
    <automated>npx vitest run components/route-planner/__tests__/TripSummary.test.tsx components/route-planner/__tests__/SpeedControl.test.tsx --reporter=verbose</automated>
  </verify>
  <done>TripSummary shows per-leg breakdown and totals. SpeedControl renders working slider 5-30 knots. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TripSummary and SpeedControl into panel and page</name>
  <files>components/route-planner/RoutePlannerPanel.tsx, app/page.tsx</files>
  <action>
1. Update `app/page.tsx`:
- Add `const [speedKnots, setSpeedKnots] = useState(10)` state
- Pass `speedKnots` and `onSpeedChange={setSpeedKnots}` to RoutePlannerPanel (replace the dummy values from Plan 01)

2. Update `components/route-planner/RoutePlannerPanel.tsx`:
- Import TripSummary and SpeedControl
- Replace placeholder slots with actual components
- Layout order in panel: StopList at top, then SpeedControl, then TripSummary
- SpeedControl receives `speedKnots` and `onSpeedChange` from props
- TripSummary receives `routeLegs` and `speedKnots` from props
- TripSummary only renders when routeLegs.length > 0 (StopList handles empty/single states)

3. Ensure time display updates immediately when speed slider changes -- this is guaranteed by React reactivity since TripSummary reads speedKnots prop directly. No A* recomputation occurs (routeLegs memoized on stops only, not speed).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose && npm run build</automated>
  </verify>
  <done>Complete route planning panel: stop list with drag-reorder, speed slider (5-30 knots), trip summary with per-leg breakdown. Speed changes instantly update all time displays without route recomputation. Build succeeds with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete route planning UI</name>
  <files>app/page.tsx</files>
  <action>
Human verifies the complete multi-stop route planning UI with drag-to-reorder stops, trip summary (distance in nm + estimated time), per-leg breakdown table, and adjustable cruise speed slider (5-30 knots).

Steps to verify:
1. Open http://localhost:3000 in browser
2. Click 3-4 ports on the map -- they should appear as numbered stops in the right panel
3. Verify per-leg distances and total distance show in nautical miles
4. Verify travel times show in "Xh Ym" format
5. Drag a stop to reorder -- route on map should update, distances/times recalculate
6. Click X on a stop -- it should be removed, route updates
7. Adjust speed slider from 10 to 20 knots -- all travel times should halve immediately
8. Resize browser narrow -- panel should collapse below map
9. With 0 stops: panel shows "Click ports on the map to add stops"
10. With 1 stop: panel shows the stop + "Add another stop" message
  </action>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>Route planning UI approved by user</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass including distance, StopList, TripSummary, SpeedControl
- `npm run build` -- clean build, no TypeScript errors
- Visual verification via checkpoint task above
</verification>

<success_criteria>
- Trip summary shows total distance (nm) and estimated travel time
- Per-leg breakdown shows From, To, distance, time for each leg
- Speed slider (5-30 knots, default 10) updates all times immediately
- No route recomputation when only speed changes (performance)
- Null path legs handled gracefully ("No route found")
- All Phase 4 requirements (ROUTE-02, ROUTE-03, ROUTE-04) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-route-planning-ui/04-02-SUMMARY.md`
</output>

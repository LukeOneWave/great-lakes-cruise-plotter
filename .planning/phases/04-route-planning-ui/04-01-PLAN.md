---
phase: 04-route-planning-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pathfinding/distance.ts
  - lib/pathfinding/types.ts
  - components/route-planner/StopItem.tsx
  - components/route-planner/StopList.tsx
  - components/route-planner/RoutePlannerPanel.tsx
  - app/page.tsx
  - components/map/NauticalMap.tsx
autonomous: true
requirements: [ROUTE-02, ROUTE-03]

must_haves:
  truths:
    - "User can click ports on the map to add them as ordered stops"
    - "User can remove a stop via X button"
    - "User can drag-and-drop to reorder stops in the side panel"
    - "Route automatically recomputes when stops change"
    - "Side panel appears on the right with stop list"
  artifacts:
    - path: "lib/pathfinding/distance.ts"
      provides: "computeDistanceNm and formatTime utility functions"
      exports: ["computeDistanceNm", "formatTime"]
    - path: "lib/pathfinding/types.ts"
      provides: "RouteLeg interface"
      contains: "interface RouteLeg"
    - path: "components/route-planner/StopList.tsx"
      provides: "Ordered stop list with drag-to-reorder and remove"
    - path: "components/route-planner/RoutePlannerPanel.tsx"
      provides: "Container panel composing StopList"
    - path: "app/page.tsx"
      provides: "Multi-stop state management with ordered array"
  key_links:
    - from: "app/page.tsx"
      to: "lib/pathfinding/route.ts"
      via: "findRoute called for each consecutive stop pair"
      pattern: "stops\\.slice.*findRoute"
    - from: "app/page.tsx"
      to: "components/route-planner/RoutePlannerPanel.tsx"
      via: "props: stops, routeLegs, onReorder, onRemove"
    - from: "components/route-planner/StopItem.tsx"
      to: "HTML5 DnD API"
      via: "draggable, onDragStart, onDragOver, onDrop"
---

<objective>
Multi-stop route builder: ordered stop management, drag-and-drop reordering, and side panel layout.

Purpose: Transform the app from a 2-port route viewer into an N-stop trip planner with reorderable stops.
Output: Working side panel with stop list, drag-to-reorder, remove buttons, and multi-leg route computation displayed on map.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-route-planning-ui/04-CONTEXT.md
@.planning/phases/04-route-planning-ui/04-RESEARCH.md
@.planning/phases/04-route-planning-ui/04-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/pathfinding/types.ts:
```typescript
export interface RoutePoint {
  lng: number;
  lat: number;
}
export interface PathResult {
  points: RoutePoint[];
  gridCellCount: number;
}
```

From lib/pathfinding/route.ts:
```typescript
export function findRoute(grid: NavigationGrid, fromPortId: string, toPortId: string): PathResult | null;
```

From lib/ports/ports.ts:
```typescript
export function getAllPorts(): Port[];
export function getPortById(id: string): Port | undefined;
export function searchPorts(query: string): Port[];
```

From lib/ports/types.ts:
```typescript
export interface Port {
  id: string;
  name: string;
  lake: string;
  lat: number;
  lng: number;
  country: string;
}
```

From components/map/NauticalMap.tsx:
```typescript
interface NauticalMapProps {
  width?: number;
  height?: number;
  ports?: Port[];
  selectedPortIds?: Set<string>;
  onPortSelect?: (portId: string) => void;
  routePoints?: RoutePoint[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Distance utilities, RouteLeg type, and multi-stop page refactor</name>
  <files>lib/pathfinding/distance.ts, lib/pathfinding/types.ts, lib/pathfinding/__tests__/distance.test.ts, app/page.tsx, components/map/NauticalMap.tsx</files>
  <behavior>
    - computeDistanceNm([{lng:-87,lat:45},{lng:-86,lat:45}]) returns ~51.5 nm (1 degree longitude at lat 45)
    - computeDistanceNm([]) returns 0
    - computeDistanceNm with single point returns 0
    - formatTime(100, 10) returns "10h 0m"
    - formatTime(5, 10) returns "30m"
    - formatTime(0, 10) returns "0m"
  </behavior>
  <action>
1. Add `RouteLeg` interface to `lib/pathfinding/types.ts`:
```typescript
export interface RouteLeg {
  from: Port;
  to: Port;
  path: PathResult | null;
  distanceNm: number;
}
```

2. Create `lib/pathfinding/distance.ts` with:
- `computeDistanceNm(points: RoutePoint[]): number` -- uses `geoDistance` from d3-geo, multiplies sum by 3440.065 (Earth radius in nm). Takes [lng, lat] pairs as geoDistance expects.
- `formatTime(distanceNm: number, speedKnots: number): string` -- returns "Xh Ym" format. If hours is 0, return just "Ym". Handle 0 distance returning "0m".

3. Create `lib/pathfinding/__tests__/distance.test.ts` with tests for the behaviors above. Write tests FIRST (red), then implement (green).

4. Refactor `app/page.tsx`:
- Replace `useState<Set<string>>` with `useState<string[]>([])` for stops
- Update `handlePortSelect`: if port already in array, remove it; otherwise append
- Add `reorderStops(fromIndex, toIndex)` callback using splice pattern from RESEARCH
- Add `removeStop(index)` callback
- Compute `routeLegs: RouteLeg[]` via `useMemo` keyed on `[stops, grid]`: for each consecutive pair, call `findRoute`, compute `computeDistanceNm` on result points
- Compute `allRoutePoints` by flatMapping leg paths for map display
- Change `selectedPortIds` prop passed to NauticalMap: convert stops array to `new Set(stops)`
- Pass `routePoints={allRoutePoints}` instead of `routeResult?.points`

5. Update `NauticalMap` props: keep `selectedPortIds` as `Set<string>` (just convert in page.tsx). No NauticalMap interface change needed since we flatten route points.

6. Add responsive flex layout to page.tsx `<main>`: `flex flex-col lg:flex-row`. Map div gets `flex-1 min-h-[400px]`. Panel div gets `w-full lg:w-80 xl:w-96 border-t lg:border-t-0 lg:border-l border-neutral-200 bg-white overflow-y-auto`.
  </action>
  <verify>
    <automated>npx vitest run lib/pathfinding/__tests__/distance.test.ts --reporter=verbose</automated>
  </verify>
  <done>Distance utilities pass all tests. page.tsx manages ordered stops array with multi-leg route computation. Layout has responsive side panel area. Map still renders with routes.</done>
</task>

<task type="auto">
  <name>Task 2: StopList with drag-and-drop reorder and remove</name>
  <files>components/route-planner/StopItem.tsx, components/route-planner/StopList.tsx, components/route-planner/RoutePlannerPanel.tsx, components/route-planner/__tests__/StopList.test.tsx, app/page.tsx</files>
  <action>
1. Create `components/route-planner/StopItem.tsx`:
- Props: `index: number`, `port: Port`, `onReorder: (from: number, to: number) => void`, `onRemove: (index: number) => void`
- "use client" directive
- HTML5 drag and drop: `draggable` attribute, `onDragStart` sets index in dataTransfer, `onDragOver` calls preventDefault and adds visual indicator CSS class, `onDragLeave` removes indicator, `onDrop` reads fromIndex and calls onReorder
- Display: drag handle icon (hamburger &#x2630;), port name (font-medium text-sm), port lake (text-xs text-neutral-500), X remove button (text-neutral-400 hover:text-red-500)
- Styling: `flex items-center gap-2 p-2 bg-white rounded border cursor-grab`, add `border-blue-400` class on dragOver for drop target feedback

2. Create `components/route-planner/StopList.tsx`:
- Props: `stops: Port[]`, `onReorder`, `onRemove`
- "use client" directive
- Renders list of StopItem components with proper keys (port.id)
- Empty state: "Click ports on the map to add stops" when stops.length === 0
- Single stop state: shows the stop + "Add another stop to plan a route" message
- Each stop shows its number (1, 2, 3...) before the drag handle

3. Create `components/route-planner/RoutePlannerPanel.tsx`:
- Props: `stops: Port[]`, `routeLegs: RouteLeg[]`, `speedKnots: number`, `onSpeedChange: (speed: number) => void`, `onReorder`, `onRemove`
- "use client" directive
- Panel header: "Route Planner" with nautical styling
- Renders StopList
- Placeholder slots for TripSummary and SpeedControl (Plan 02 will fill these)
- Tailwind styling: `p-4 flex flex-col gap-4 h-full`

4. Create `components/route-planner/__tests__/StopList.test.tsx`:
- Test: renders empty state message when no stops
- Test: renders port names when stops provided
- Test: remove button calls onRemove with correct index

5. Wire RoutePlannerPanel into `app/page.tsx`:
- Import RoutePlannerPanel
- Resolve stops array to Port objects via `getPortById` in a useMemo
- Pass stops (as Port[]), routeLegs, reorderStops, removeStop
- Pass speedKnots=10 and dummy onSpeedChange for now (Plan 02 adds real speed state)
  </action>
  <verify>
    <automated>npx vitest run components/route-planner/__tests__/StopList.test.tsx --reporter=verbose && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Side panel shows ordered stop list. Users can add stops by clicking map ports, remove via X button, and drag-to-reorder. Route recomputes on any stop change. Empty and single-stop states display helpful messages.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npm run build` -- no TypeScript errors
- Visual: app shows map on left, route planner panel on right
- Click 3+ ports: stops appear in panel, route draws through all stops on map
- Drag a stop to reorder: route updates
- Click X on a stop: stop removed, route updates
</verification>

<success_criteria>
- Multi-stop selection works (click ports to add/remove from ordered list)
- Drag-and-drop reorder changes stop order and recomputes route
- Remove button works on each stop
- Route displays on map through all stops in order
- Side panel responsive: right side on desktop, below map on mobile
- Distance utility functions tested and working
</success_criteria>

<output>
After completion, create `.planning/phases/04-route-planning-ui/04-01-SUMMARY.md`
</output>

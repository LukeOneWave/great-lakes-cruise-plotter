---
phase: 05-export-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/export/export-utils.ts
  - lib/export/svg-export.ts
  - lib/export/png-export.ts
  - lib/export/pdf-export.ts
  - lib/export/__tests__/svg-export.test.ts
  - lib/export/__tests__/png-export.test.ts
  - lib/export/__tests__/pdf-export.test.ts
  - components/map/NauticalMap.tsx
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03]

must_haves:
  truths:
    - "exportSVG() serializes the SVG DOM node to a downloadable .svg file with all defs inlined"
    - "exportPNG() renders SVG to Canvas at 2x resolution and triggers PNG download"
    - "exportPDF() creates a landscape A4 PDF with the map image and optional trip summary text"
  artifacts:
    - path: "lib/export/export-utils.ts"
      provides: "triggerDownload helper, SVG cloning/prep, renderToCanvas shared function"
      exports: ["triggerDownload", "prepareSvgForExport", "renderToCanvas"]
    - path: "lib/export/svg-export.ts"
      provides: "SVG serialization and download"
      exports: ["exportSVG"]
    - path: "lib/export/png-export.ts"
      provides: "SVG-to-Canvas-to-PNG pipeline"
      exports: ["exportPNG"]
    - path: "lib/export/pdf-export.ts"
      provides: "PNG-to-PDF via jsPDF"
      exports: ["exportPDF"]
  key_links:
    - from: "lib/export/png-export.ts"
      to: "lib/export/export-utils.ts"
      via: "imports renderToCanvas and triggerDownload"
      pattern: "import.*renderToCanvas.*export-utils"
    - from: "lib/export/pdf-export.ts"
      to: "lib/export/export-utils.ts"
      via: "imports renderToCanvas for shared canvas pipeline"
      pattern: "import.*renderToCanvas.*export-utils"
---

<objective>
Build the three export functions (SVG, PNG, PDF) and add forwardRef to NauticalMap so the SVG element is accessible for export.

Purpose: Core export logic that Plan 02's UI will call. Separation keeps export logic testable and UI-independent.
Output: Four export library files, three test files, and NauticalMap with forwardRef.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-pipeline/05-RESEARCH.md

@components/map/NauticalMap.tsx
@components/map/MapDefs.tsx
@components/map/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export utility functions and NauticalMap forwardRef</name>
  <files>lib/export/export-utils.ts, components/map/NauticalMap.tsx</files>
  <action>
1. Create `lib/export/export-utils.ts` with three exports:
   - `triggerDownload(blob: Blob, filename: string): void` -- creates anchor element, sets href to ObjectURL, clicks, cleans up (revokes URL, removes anchor)
   - `prepareSvgForExport(svgElement: SVGSVGElement): SVGSVGElement` -- clones the SVG node (deep clone), sets xmlns and xmlns:xlink attributes on the clone, returns the clone. Do NOT modify the live DOM.
   - `renderToCanvas(svgElement: SVGSVGElement, scale: number): Promise<HTMLCanvasElement>` -- serializes SVG via XMLSerializer, creates Blob URL, loads into Image, draws onto canvas at `scale`x dimensions, revokes URL, returns canvas. This is the shared pipeline used by both PNG and PDF export.

2. Update `components/map/NauticalMap.tsx`:
   - Import `forwardRef` from React
   - Wrap the component with `forwardRef<SVGSVGElement, NauticalMapProps>`
   - Attach the forwarded ref to the `<svg>` element
   - Keep the existing `NauticalMap` export name (use `forwardRef` display name)
   - Change nothing else about the component's behavior
  </action>
  <verify>
    <automated>npx vitest run components/map/__tests__/NauticalMap.test.tsx -x</automated>
  </verify>
  <done>export-utils.ts exports three functions. NauticalMap accepts a ref that attaches to the SVG element. Existing NauticalMap tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SVG, PNG, and PDF export functions with tests</name>
  <files>lib/export/svg-export.ts, lib/export/png-export.ts, lib/export/pdf-export.ts, lib/export/__tests__/svg-export.test.ts, lib/export/__tests__/png-export.test.ts, lib/export/__tests__/pdf-export.test.ts</files>
  <behavior>
    - SVG: exportSVG(svgEl, "map.svg") calls prepareSvgForExport, serializes with XMLSerializer, creates Blob with type image/svg+xml, calls triggerDownload
    - PNG: exportPNG(svgEl, "map.png") calls renderToCanvas with scale=2, calls canvas.toBlob with image/png, calls triggerDownload
    - PDF: exportPDF(svgEl, "map.pdf", tripSummary?) calls renderToCanvas with scale=2, gets dataURL, creates jsPDF landscape A4, calls addImage filling page with 10mm margins, optionally adds trip summary text at bottom, calls pdf.save
    - All functions are async (PNG and PDF return Promise<void>, SVG can be sync)
  </behavior>
  <action>
1. Create test files first (RED phase). In jsdom environment, mock:
   - XMLSerializer (return a string)
   - URL.createObjectURL / revokeObjectURL
   - document.createElement for canvas (mock getContext returning a mock ctx with scale/drawImage) and anchor
   - Image constructor (fire onload synchronously or via Promise)
   - HTMLCanvasElement.prototype.toBlob (call callback with a mock Blob)
   - jsPDF (mock constructor, addImage, setFontSize, text, save, internal.pageSize)
   Tests should verify: correct function calls in order, correct arguments (scale, mime types, filenames).

2. Create `lib/export/svg-export.ts`:
   - `export async function exportSVG(svgElement: SVGSVGElement, filename = 'great-lakes-cruise.svg'): Promise<void>`
   - Uses prepareSvgForExport to clone, XMLSerializer to serialize, Blob to package, triggerDownload to save

3. Create `lib/export/png-export.ts`:
   - `export async function exportPNG(svgElement: SVGSVGElement, filename = 'great-lakes-cruise.png'): Promise<void>`
   - Uses renderToCanvas(svgElement, 2) from export-utils
   - Wraps canvas.toBlob in a Promise, calls triggerDownload

4. Create `lib/export/pdf-export.ts`:
   - `export async function exportPDF(svgElement: SVGSVGElement, filename = 'great-lakes-cruise.pdf', tripSummary?: string): Promise<void>`
   - Uses renderToCanvas(svgElement, 2), gets canvas.toDataURL('image/png')
   - Creates `new jsPDF({ orientation: 'landscape', unit: 'mm', format: 'a4' })`
   - Calculates page dimensions, 10mm margins, reserves 15mm at bottom for trip summary if provided
   - Calls addImage with PNG data filling available area
   - If tripSummary provided: setFontSize(10), text(tripSummary, margin, pageHeight - margin)
   - Calls pdf.save(filename)
  </action>
  <verify>
    <automated>npx vitest run lib/export/__tests__/ -x</automated>
  </verify>
  <done>All three export functions exist with correct signatures. Tests pass verifying serialization, canvas pipeline, and jsPDF calls. No browser required -- all browser APIs mocked.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- full test suite passes (existing + new export tests)
- lib/export/ directory contains 4 source files and 3 test files
- NauticalMap still renders correctly (existing tests green)
</verification>

<success_criteria>
- exportSVG, exportPNG, exportPDF functions are importable and tested
- NauticalMap exposes SVG element via forwardRef
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-pipeline/05-01-SUMMARY.md`
</output>

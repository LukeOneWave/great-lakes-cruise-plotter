---
phase: 05-export-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - components/ui/ExportMenu.tsx
  - app/page.tsx
autonomous: false
requirements: [EXP-01, EXP-02, EXP-03]

must_haves:
  truths:
    - "User sees an export button/dropdown on the page"
    - "User can click to download SVG, PNG, or PDF of their map"
    - "Exported files contain the nautical map with routes, ports, and styling"
    - "Loading indicator shows during PNG/PDF export"
  artifacts:
    - path: "components/ui/ExportMenu.tsx"
      provides: "Export dropdown UI with three format options and loading state"
      exports: ["ExportMenu"]
    - path: "app/page.tsx"
      provides: "Wiring: svgRef passed to NauticalMap and ExportMenu"
  key_links:
    - from: "app/page.tsx"
      to: "components/map/NauticalMap.tsx"
      via: "svgRef passed as ref prop"
      pattern: "ref=.svgRef"
    - from: "components/ui/ExportMenu.tsx"
      to: "lib/export/svg-export.ts"
      via: "imports exportSVG"
      pattern: "import.*exportSVG"
    - from: "components/ui/ExportMenu.tsx"
      to: "lib/export/png-export.ts"
      via: "imports exportPNG"
      pattern: "import.*exportPNG"
    - from: "components/ui/ExportMenu.tsx"
      to: "lib/export/pdf-export.ts"
      via: "imports exportPDF"
      pattern: "import.*exportPDF"
---

<objective>
Create the ExportMenu UI component and wire it into the page with the SVG ref so users can download their maps.

Purpose: Connects the export functions from Plan 01 to user-facing UI, completing the export pipeline.
Output: ExportMenu component and updated page.tsx with full export capability.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-pipeline/05-01-SUMMARY.md

@app/page.tsx
@components/map/NauticalMap.tsx
@components/route-planner/RoutePlannerPanel.tsx
</context>

<interfaces>
<!-- From Plan 01 exports -->
From lib/export/svg-export.ts:
```typescript
export async function exportSVG(svgElement: SVGSVGElement, filename?: string): Promise<void>;
```

From lib/export/png-export.ts:
```typescript
export async function exportPNG(svgElement: SVGSVGElement, filename?: string): Promise<void>;
```

From lib/export/pdf-export.ts:
```typescript
export async function exportPDF(svgElement: SVGSVGElement, filename?: string, tripSummary?: string): Promise<void>;
```

NauticalMap now accepts forwardRef<SVGSVGElement>.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ExportMenu component and page wiring</name>
  <files>components/ui/ExportMenu.tsx, app/page.tsx</files>
  <action>
1. Create `components/ui/ExportMenu.tsx` ("use client"):
   - Props: `{ svgRef: React.RefObject<SVGSVGElement | null>; tripSummary?: string }`
   - State: `isExporting: boolean`, `isOpen: boolean` (dropdown open/closed)
   - Render a button labeled "Export Map" with a down-arrow icon (use a simple SVG chevron, no icon library)
   - On click, toggle dropdown showing three options: "Download SVG", "Download PNG", "Download PDF"
   - Each option calls the corresponding export function with svgRef.current
   - Guard: if svgRef.current is null, disable the button
   - During PNG/PDF export (async), set isExporting=true, show a spinner or "Exporting..." text on the button, disable all options
   - On completion or error, set isExporting=false
   - Wrap each export call in try/catch -- on error, console.error (no toast needed for v1)
   - Close dropdown after clicking an option
   - Close dropdown on click-outside (useEffect with document click listener)
   - Style with Tailwind: position absolute dropdown, white background, shadow-lg, rounded, z-50
   - Place export button styling consistent with existing UI (neutral colors, hover states)

2. Update `app/page.tsx`:
   - Import `useRef` (add to existing React imports)
   - Create `const svgRef = useRef<SVGSVGElement>(null)`
   - Pass `ref={svgRef}` to `<NauticalMap>`
   - Import and render `<ExportMenu svgRef={svgRef} tripSummary={tripSummaryString} />`
   - For tripSummary: construct a string from the existing route/trip state summarizing stops, total distance, and time (if a route exists). If no route, pass undefined.
   - Position ExportMenu in the top-right area of the page or within the route planner panel header area -- use your judgment for best UX placement
  </action>
  <verify>
    <automated>npx vitest run -x && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>ExportMenu renders with three format options. svgRef connects NauticalMap to export functions. Build succeeds. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete export pipeline: SVG, PNG, and PDF download from the map page</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Add 2-3 stops to create a route on the map
    3. Click the "Export Map" button
    4. Download SVG -- open in browser tab, verify nautical styling, route lines, port markers visible
    5. Download PNG -- open the file, verify it is crisp (zoom in), contains full map with route
    6. Download PDF -- open the file, verify landscape layout, map fills the page, trip summary text appears at bottom
    7. Verify loading indicator appears briefly during PNG/PDF export
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with any export format</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` -- full test suite green
- `npx next build` -- builds without errors
- Manual: all three export formats produce correct output with nautical styling preserved
</verification>

<success_criteria>
- User can export map as SVG, PNG, and PDF from the UI
- Exported files preserve nautical styling, routes, ports, and labels
- Loading indicator during async exports
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-pipeline/05-02-SUMMARY.md`
</output>
